# Seis-Codec training environment
# One-click setup from repo root:
#   bash seis-codec/setup_env.sh
#
# Or from this directory:
#   bash setup_env.sh

# PyTorch (CUDA 12.4). For CPU-only, run setup_env.sh with USE_CPU=1
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.1.0
torchaudio>=2.1.0

# Training
lightning>=2.0.0
ml_collections>=0.1.1
tensorboard>=2.14.0

# SeisBench / data
seisbench>=0.4.0
numpy>=1.23.0
scipy>=1.10.0
pandas>=1.5.0
scikit-learn>=1.2.0
h5py>=3.8.0

# DAC (also installed editable via setup_env.sh)
einops>=0.6.0
argbind>=0.3.7
descript-audiotools>=0.7.2
tqdm>=4.65.0

# seisLM imports (dataloaders / project_path)
GitPython>=3.1.0

# Evaluation plots (evaluate.py)
matplotlib>=3.7.0

# Optional: only needed for --use_task_aware_loss
# torchtune>=0.4.0
# torchao>=0.5.0
# transformers>=4.36.0

# Dataset cache: export SEISBENCH_CACHE_ROOT=/path/with/22GB+ free space
